#define MIN_PASS_LENGTH	6
#define MAX_PASS_LENGTH	18

forward ShowRegisterEmail(playerid, dialogid, response, listitem, string:inputtext[]);
forward ShowRegisterGender(playerid, dialogid, response, listitem, string:inputtext[]);